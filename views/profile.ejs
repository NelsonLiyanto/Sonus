<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-T3c6CoIi6uLrA9TneNEoa7RxnatzjcDSCmG1MXxSR1GAsXEV/Dwwykc2MPK8M2HN" crossorigin="anonymous">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js" integrity="sha384-C6RzsynM9kWDrMNeT87bh95OGNyZPhcTNXj1NW7RuBCsyN/o0jlpcV8Qyq46cDfL" crossorigin="anonymous"></script>
    <link rel="stylesheet" href="https://fonts.googleapis.com/css?family=Italiana">
    <style>

    </style>
    <title>Sonus</title>
</head>
<body data-bs-theme="dark" style="font-family: Italiana;" >
    <%- include('./components/navbar',{path:profile}) %>
    <div class="container mt-5"> 
        <div class="row justify-content-center text-center">
            <div class="col-5">
                <h1><%= profile.name %></h1>
                <img src="<%= profile.pfpUrl %>" class="rounded" alt="" style="height: 200px;">
                <h5 class="my-3"><%= profile.description %></h3>
                <a href="/sonus/profile/edit" class="btn btn-success">Edit Profile</a>
                <h4 class="my-3">Playlist:</h4>
            </div>
        </div>
        <% if(!playlist) {%>
            <div class="row justify-content-center text-center">
                <div class="col-3">
                <a href="/sonus/profile/playlist/add" class="btn btn-primary">Create Playlist</a>

                </div>
            </div>
            <% } %>

        <% if(playlist) {%>
            <div class="row justify-content-center text-center">
                <div class="card mb-3" style="max-width: 540px;">
                  <div class="row g-0">
                    <div class="col-md-4">
                      <img src="<%= playlist.coverUrl %>" class="img-fluid rounded-start" alt="...">
                    </div>
                    <div class="col-md-8 ">
                      <div class="card-body ">
                        <h5 class="card-title"><%= playlist.name %> <%= emoji %></h5>
                        <p class="card-text"><%= playlist.description %></p>
                        <p class="card-text"><%= playlist.getPlaylistCode() %></p>
                        
                        <ol>
                          <% for(content of playlist.PlaylistContents) {%>
                              <li><%= content.Song.title %> by <%= content.Song.artist %></li>
                              <% } %>
                        </ol>
                        <p class="card-text"><small class="text-body-secondary">Created by: <%= profile.name %></small></p>
                        <a href="/sonus/profile/playlist/delete" class="btn btn-danger">Delete</a>
                      </div>
                    </div>
                  </div>
                </div>
          </div>
            <% } %>

    </div>

</body>
</html>